apiVersion: v1
kind: Service
metadata:
  name: service-app
  namespace: khushi
spec:
  selector:
    app: book-my-show
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort
